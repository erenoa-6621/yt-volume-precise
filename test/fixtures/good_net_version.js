'use strict';
// fixture: 違反なし。生 IP 検査の「過検知」を検知するための対照検体。
// 版数・日付めいた表記が引用符の中にあっても、4オクテットの IP ではないので違反ではない。
var ver = "//1.2";          // 3オクテット未満
var rev = '//10.4';         // 同上
var seq = "1.2.3.4";        // IP 形だが // が無い（プロトコル相対 URL ではない）
var note = 'a//1.2.3';      // 引用符の直後ではない上に3オクテット
// 版 //2026.7.17 のような行コメントも違反ではない（引用符が前に無い）。
var path = '/watch';        // 単スラッシュの相対パスは許可
