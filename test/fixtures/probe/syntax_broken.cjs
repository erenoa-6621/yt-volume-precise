// verify.sh 項目2 の陽性対照（意図的に壊してある。直さないこと）。
//
// これは「node --check が本当に走ったか」を確かめるための検体である。
// node が関数などで置き換えられて何もしなくなると、壊れたこのファイルすら
// 「通った」ことになる。verify.sh はこの検体が **落ちること** を要求する。
//
// 拡張子が .js ではなく .cjs なのは、verify.sh 項目2 の本走査（*.js 全数）が
// この意図的な壊れ物を拾って自分自身を赤くしないようにするため。
function broken( {
  return
