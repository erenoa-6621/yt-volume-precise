# INSTALL.md — YT Volume Precise 導入と動作確認の手引き

対象読者: この拡張を自分の Chrome に入れて動かしてみる**あなた**。
上から順に実行すれば導入が終わり、同時に「どこまで動いてどこで壊れたか」が手元に記録として残る。
落ちた項目は issue にそのまま貼れる形にしてあります。

## 0. これは何か

YouTube の音量を **数値入力とプリセット**で操作する Chrome 拡張（Manifest V3）。
作った理由は機能ではなく**権限**にある。既存の音量ブースター拡張は全サイトの閲覧権限
（`<all_urls>` + `tabs`）を要求するのが常態で、音量を変えたい対価に閲覧履歴を渡す取引になっている。
本拡張は `storage` と YouTube 2ホストしか要求しない（実物は manifest.json:6-10 `"host_permissions"`。
README.md にも同趣旨の節がありますが、節の名前は変わりえます）。

> この文書の根拠は原則 `ファイル名:行番号` で示します（2026-09-03 に全数を実測して付け直したもの）。
> ただし `verify.sh` と `README.md` と `tools/` の中身は改訂で行番号が動きやすいため、
> **そこだけは行番号ではなく内容で引いています。** 行番号が外れた引用は根拠になりません。
>
> **`ファイル名:行番号` の引用には、隣に「アンカー語」（バッククォートの識別子、または 「…」 の逐語）を
> 必ず添えてあります。** 文書引用の検査（`tools/scan_doc_cites.sh`）は、行番号が範囲内かだけでなく
> **その語が引用先の行に本当に在るか**まで照合します。実装で行がずれれば `verify.sh` の項目12 が赤くなります。
> アンカー語の無い引用は、行がずれても**ファイルの行数に収まっていれば素通り**していました
> （＝機械に見えない腐り。2026-09-03 に複数回の独立した実測で確認）。
>
> **一方、「内容での引用」（行番号を伴わない実装説明）は依然として機械検査の外にあります。**
> 「`verify.sh` は〜を呼ぶ」のような記述は検知できません。
> 現にこの文書は、通信スキャンの走査対象を「`src/` 固定」と書き続けていました
> （**すでに塞がれた穴の状態を読者に説明していた**。2026-09-02 に独立検証で発見）。
> そのため内容での引用は **項目番号・件数・逐語の文言に依存しない書き方**へ寄せてあります。
> **こちらが腐るのを止める機構は、まだありません。**

---

## 1. 入れる前に知っておくこと（先に不利な事実を出す）

- **実機で確認できているのは一部だけです。**
  自動検査はすべて Node 上の偽 DOM / 偽 chrome で行われている
  （test/helpers/wiring.js:100 に「実物の page.js は movie_player と Web Audio に触るので
  Node では動かせない」と明記されている）。ブラウザでしか分からないことは、そこでは何も分からない。
  実機で確かめたのは Chrome の `www.youtube.com` における基本操作
  （パネルの開閉・数値入力・プリセット・プレイヤー内ボタンの表示位置）までで、
  **それ以外の項目は未確認です。** とくに 100 を超えるブースト（Web Audio）が実際にどう鳴るか、
  `music.youtube.com`、実際の通信の中身は誰も見ていません。
  **チェックリストは落ちる項目があって当然と考えてください。**
- インストール方法は「パッケージ化されていない拡張機能を読み込む」だけ。
  Chrome ウェブストアには**提出していません**。
- **外部に何も送信しない。** これは口約束ではなく `verify.sh` の通信スキャン（`tools/scan_network.sh`）が
  機械検査している。**走査対象は `src/` 配下に固定されていません。**
  どのファイルを見るかは `tools/list_scan_targets.sh` が決めていて、manifest が参照するファイルと、
  リポジトリ内のコードの拡張子（js / mjs / cjs / html / htm / css / json）のファイルを集めます
  （`test/` は意図的な違反検体、`tools/` は検査器自身なので除外。理由はその収集スクリプトに書いてある）。
  対象が0件になったら収集規則が壊れたとみなして失敗します（fail-closed）。
  何件を走査したかは `verify.sh` を実行したときの通信スキャンの行に出るので、実数はそこで見てください。
  ただし見ているのは**ディスク上のコードと manifest の静的検査**であって、
  「Chrome が動かしたときの実際の通信」は見ていない。実通信をキャプチャした人はまだ居ない（未確認）。
  さらに静的検査は**既知の API 名の字面しか見ません**。文字列を組み立てて API を再構成するコード
  （`globalThis["fet" + "ch"]` の類）は原理的に捕まりません。検査器自身が冒頭でそう明記しています。
  **これは仕掛け線であって証明ではない。**
- **対応ブラウザは Chrome と Edge（Chromium 系）。Firefox は未対応。**
  根拠は推測ではなく manifest とコード：manifest.json に Firefox 用の
  `browser_specific_settings` が無く、コードは `chrome.*` 名前空間のみを使う
  （src/content.js:166 `chrome.storage.local`、src/popup.js:102 `chrome.tabs.query`）。
  Firefox で動かした人は居ないので、動くか動かないかも未確認です。
- 所要時間は 10〜20 分（読み込み 2 分、チェックリスト 10 分、記録 5 分）を見込んでください。
  **ただし途中で壊れた場合はここに収まりません。**

---

## 2. 読み込む対象フォルダ（フルパス）

```
<リポジトリを clone した場所>/yt-volume-precise
```

このフォルダの直下に `manifest.json` がある。**選ぶのはこのフォルダ自体**（`src/` ではない）。

### 2-1. WSL のパスを Windows の Chrome から開く（WSL で開発している場合）

Windows 側の Chrome から WSL 内のフォルダを指すには、エクスプローラ形式のパスを使う。

```
\\wsl$\<ディストリビューション名>\home\<ユーザー名>\<clone した場所>\yt-volume-precise
```

`\\wsl$\` は古い形式で、新しい Windows では `\\wsl.localhost\` も使える。**両方試してよい。**

**ディストリビューション名の確認方法**（決めつけない。自分で確かめてください）:

- WSL のシェルで `echo $WSL_DISTRO_NAME`
- Windows の PowerShell またはコマンドプロンプトで `wsl.exe -l -v`
- エクスプローラのアドレス欄に `\\wsl$\` とだけ入れて Enter → 一覧が出るのでその中から選ぶ

参考（2026-09-02 に実測した一例。**環境ごとに違うので必ず自分で確認すること**）:
`$WSL_DISTRO_NAME` が `Ubuntu-22.04` で、`wsl.exe -l -v` には `Ubuntu` と `Ubuntu-22.04` の
**2つが並んでいた**。名前だけ見て `Ubuntu` を選ぶと空振りします。

**代替手順（UNC パスで読み込めない場合）**: リポジトリを Windows 側へコピーしてそちらを読み込む
（例: WSL で `cp -r <clone した場所>/yt-volume-precise /mnt/c/Users/<ユーザー名>/ytvp` してから
`C:\Users\<ユーザー名>\ytvp` を選ぶ）。**コピー先を編集しても WSL 側には反映されません。**

> Chrome が `\\wsl$\` 形式のネットワークパスから未パッケージ拡張を読み込めるかは**未確認**です。
> 拒否される・極端に遅い・ファイル変更を検知しない、といった**動かない可能性がある**ので、
> 詰まったら粘らずに上のコピー手順へ切り替えてください。

---

## 3. 導入手順（実際の画面の言葉で）

1. Chrome のアドレス欄に `chrome://extensions` と入力して開く。
   （Edge の場合は `edge://extensions`。以降の文言は Chrome のものです。）
2. 画面**右上**の「**デベロッパー モード**」のスイッチを **ON** にする。
3. 左上に現れる「**パッケージ化されていない拡張機能を読み込む**」を押す。
4. フォルダ選択ダイアログのアドレス欄に §2 のパスを貼り付け、
   `yt-volume-precise` フォルダを選んで「フォルダーの選択」を押す。
5. 一覧に「**YT Volume Precise**」（`"version": "0.2.0"` / manifest.json:3-4）のカードが出れば読み込み成功。
6. **すでに開いている YouTube のタブがあれば、必ずリロードする。**
   content script は拡張を入れた後に読み込まれたページにしか入りません。
   **これを忘れると、正常な拡張でもポップアップが「YouTube を開いてください」になります。**
   チェックリストで最初に疑うべきはここです。

---

## 4. ★動作確認チェックリスト（この文書の本体）

**上から順に実施してください。** 途中で落ちたら、そこから先は結果が信用できないので、
落ちた項目と画面・コンソールの内容を控えて、不具合として記録してください（issue にそのまま貼れます）。

各項目の `[ ]` を埋め、動かなかったものには実際に見えたものを書き足すこと。

**番号が 11 から 13 へ飛びます。** 12番は「チャンネル別の音量記憶」でしたが、
「必要ない機能」と判断して**機能ごと廃止**したので項目も消しました。
他の文書（`KNOWN_GAPS.md`）が項目番号でこの節を参照しているため、残りの番号は振り直していません。

---

### 4-1. 読み込みと表示

- [ ] **1. 拡張が読み込めた（赤い「エラー」ボタンが出ない）**
  - 期待: `chrome://extensions` に「YT Volume Precise 0.2.0」のカードが出て、赤い「エラー」表示が無い。
  - 動かなかったら: manifest が参照するファイルの不足か JSON の書式エラー。
    manifest が指すのは `src/lib/volume.js` `src/overlay.js` `src/content.js` `src/overlay.css`
    `src/popup.html` `src/page.js` の6本（manifest.json:17-38 に `"src/lib/volume.js"` などが並ぶ）。フォルダを1つ上（または下）で
    選び間違えている可能性が最も高い。

- [ ] **2. YouTube の動画ページを開き、ツールバーの拡張アイコンからポップアップが開く**
  - 手順: `www.youtube.com/watch?v=...` の動画ページを開く → ツールバーのパズルピース型
    「拡張機能」ボタン → 一覧から「YT Volume Precise」を押す（ピン留めしておくと以降が楽）。
  - 期待: 幅 320px 前後の黒い小さなパネルが開く。
    **v0.7 で中身が変わりました。ポップアップは自前の UI を持たなくなり、
    プレイヤー内パネルとまったく同じ部品を表示します**（二重実装をやめたため。
    ポップアップに残る自前の要素は案内文だけ ―― `class="ytvp-popup-msg"` / src/popup.html:19、
    UI を描くのは `overlay.js` / src/popup.html:21）。
    見えるのは、大きな数値の**入力欄**と `%`（**その行の右端に ⚙**＝プリセットの編集）、
    スライダー、プリセットの pill、ブーストのトグルです。
    **「適用」ボタンと独立したプリセット編集欄はもうありません**（編集欄は ⚙ の中）。
  - 動かなかったら: **manifest にアイコンが設定されていない**（`icons` キーが無い）ので、
    ツールバーには Chrome が自動生成した仮アイコンが出るはずです。見つけにくいだけの可能性が高い。
    パネル自体が開かない・真っ白なら popup.html / popup.css / popup.js のパスずれ。

---

### 4-2. ★最大の関門（ここが落ちたら設計前提の誤り）

- [ ] **3. ポップアップが現在の音量を数値で表示している**
  - 期待: 開いた瞬間は「接続中…」の1行だけで、**パネルはまだ出ません**
    （`MSG_CONNECTING` / src/popup.js:24）。**1.5 秒以内に**メッセージが消え、
    現在値の入った panel（§4 項目2 の姿）に入れ替わる。
    **v0.7 の変更点**: 疎通できるまで mount しません。空のパネルを先に出すと、
    効かない操作を並べることになるためです。
  - 動かなかったら（「接続中…」が「**YouTube を開いてください**」に変わり、
    パネルが出ない ＝ `MSG_NO_YT` / src/popup.js:25）:
    **これがこの拡張にとって最重要の失敗です。issue を立てる価値があります。**
    本拡張は「`chrome.tabs.query` は `tabs` 権限なしでも、host_permissions を持つ
    YouTube タブについては `url` を返す」という前提の上に立っています
    （SPEC.md:86-88 `chrome.tabs.query`、src/popup.js:102 `chrome.tabs.query`、
    src/popup.js:304 `isYouTubeTabUrl(url)` で `tab.url` を判定に使用）。
    **この前提を実機で確認した人は誰も居ません。** 外れていれば、`url` が空になって
    必ずオフライン表示に落ちます。設計の前提が誤っていたことになるので、
    「機能が動かない」ではなく「**SPEC 3-b の前提が実機で成立しなかった**」という形で記録してください。
  - 切り分け: 先に §3 の手順6（タブのリロード）をやったか確認する。
    それでも駄目なら §5 の DevTools で `tab.url` が取れているかを見る。
  - なお、ホスト判定は `www.youtube.com` と `music.youtube.com` の **https のみ**を通します
    （`YT_HOSTS` / src/popup.js:34）。他のサブドメイン（`m.youtube.com` 等）では設計どおりオフラインです。

---

### 4-3. ポップアップからの操作

- [ ] **4. 数値を入力して Enter → 音量が変わり、YouTube 自身の音量ゲージも動く**
  - 手順: 大きな数字（それ自体が入力欄です）に `30` と入れて Enter。
    **v0.7 で「適用」ボタンは無くなりました。確定は Enter だけです**
    （`commitInput` / src/overlay.js:492）。
  - 期待: 大きな数字が `30` になり、耳で聞こえる音量も下がる。
    さらに **YouTube プレーヤー左下のスピーカー横のゲージも 3 割ほどに縮む**。
    `<video>.volume` ではなく `movie_player.setVolume()` を叩いているため
    （`player.setVolume(playerPart)` / src/page.js:116、
    `document.getElementById('movie_player').setVolume()` / SPEC.md:174-175）、YouTube の UI と連動するはずです。
  - 動かなかったら:
    - 数字は変わるが音が変わらない → `movie_player` は見つかっているが `setVolume` が
      効いていない。YouTube 側の実装変更を疑う。
    - 数字が変わらず、打った値が**元の数字に戻る** → 入力が整数として解釈されていない。
      **小数（`87.5`）と全角の `％` は仕様上わざと弾きます**（`parseVolumeInput` / SPEC.md:64-65）。
      **v0.7 では文言は出ません。解釈できなければ黙って現在値へ戻すだけです**
      （`revertInput` / src/overlay.js:487-489）。上限はブーストの状態で変わります
      （OFF なら 100、ON なら 300。`MAX_BOOST` / src/overlay.js:5）。
    - 「プレーヤー未検出」と出る → `document.getElementById('movie_player')` が取れていない
      （`getElementById('movie_player')` / src/page.js:35-38）。
  - **YouTube のゲージが連動するかどうかは未確認です。** 連動しなくても数値だけ効くことがありえます。

- [ ] **5. プリセットのボタンで音量が変わる**
  - 期待: pill は既定で `15` `30` `50` `70` `100` の5個（`DEFAULT_PRESETS` / src/lib/volume.js:7）。
    押すとその値になり、**現在値と一致する pill だけが塗られる**（`pill--on` / src/overlay.js:790）。
  - 動かなかったら: §4 項目4 が通っていれば経路は同じなので、pill の生成
    （`ytvp-pill` / src/overlay.js:285-298）か設定の読み込みを疑う。

---

### 4-4. プレーヤー上のオーバーレイ

- [ ] **6. プレーヤー上のオーバーレイ（既定は折りたたみ）が出る**
  - **v0.2 の注意**: `www.youtube.com` でネイティブボタン（§4-8）が差し込めた場合、
    **この右上の浮きハンドルは出ません**（両方は出さない。ネイティブモードではハンドルを
    生成しない実装。コメントは src/overlay.js:223-224 `ネイティブモードでは浮きハンドルを出さない`、
    分岐は src/overlay.js:230-250 `ytvp-native-btn`）。
    §4-8 項目13 が通っているなら、この項目は「ハンドルの代わりにネイティブボタンで
    パネルが開くこと」を確認して通してください。浮きハンドルの姿を確認できるのは、
    ネイティブボタンが出なかったとき（フォールバック時）と `music.youtube.com` です。
  - 期待: 動画プレーヤーの**右上（上端・右端から 12px）**に、幅 44px・高さ 26px 程度の
    小さな半透明のハンドルがあり、`100%` のように現在値が出ている
    （`.ytvp-root` / src/overlay.css:46-49、`.ytvp-handle` / src/overlay.css:75-93）。
    既定は薄め（不透明度 0.85）で、マウスを乗せるとはっきりする。
  - **開き方は v0.6 で変わりました。マウスを乗せると開き、クリックすると固定されます**
    （YouTube 純正の 🔊 と同じ作法）。開いたパネル（幅 220px）には大きな数値の**入力欄**・
    スライダー・プリセット pill・ブーストボタンが縦に並ぶ。
    **⚙ は最上段（数値の行）の右端です**（実機で確認した結果、移しました。パネルは上へ開くので、
    末尾に置くと ⚙ が YouTube のシークバーと押し合いました）
    （`ytvp-panel` / src/overlay.js:252-255、`.ytvp-panel` / src/overlay.css:190-191）。
    **開閉そのものは §4-11、入力欄は §4-9、⚙ は §4-12 で見ます。**
    この項目で見るのは「ハンドルが在るか」「パネルが出るか」だけです。
  - 動かなかったら:
    - 何も出ない → コンソールに `[YTVP] オーバーレイ未実装のためスキップします（本体機能は動きます）。`
      （src/content.js:325）や `[YTVP] オーバーレイの mount に失敗しました`（src/content.js:348）が
      出ていないか見る。
    - 画面の右上（プレーヤーの外）に出る → `#movie_player` が見つからず `document.body` に
      フォールバックした状態（`host = doc.body` / src/overlay.js:170-174）。動作としては生きているが配置は想定外。
    - プレーヤーの他の要素の下に隠れる可能性がある（`z-index: 60`、src/overlay.css:50）。
      **YouTube 側の重なり順に勝てるかは未確認です。**

- [ ] **7. オーバーレイ上でホイールを回すと 1 刻み、Shift + ホイールで 5 刻み**
  - 期待: **オーバーレイの上にマウスカーソルがあるときだけ**効く（折りたたみのハンドルの上でも可）。
    上に回すと増、下に回すと減（`d < 0 ? base + step : base - step` / src/lib/volume.js:100-103）。
    Shift 併用で 5 刻み（`opts.shift === true ? 5 : 1` / src/lib/volume.js:98）。
    ページのスクロールは止まる（`preventDefault` / src/overlay.js:689-696）。
  - 動かなかったら: ホイールの所有者は overlay ただ一人で、`src/content.js` は wheel リスナを
    持たない契約です（`window.YTVP.stepFromWheel` / SPEC.md:223）。この契約は `verify.sh` の契約検査
    （`tools/scan_contract.sh nowheel`）が機械検査しています。
    **オーバーレイの外でホイールを回しても音量は変わりません。それは仕様です。**
  - 共有ロジック（`window.YTVP`）が読めていないと**黙って何も起きません**
    （`共有ロジックが無ければホイールは諦める` / src/overlay.js:83、`共有ロジック不在` / src/overlay.js:692）。
    その場合はコンソールに `[YTVP] volume.js が読み込まれていません。中止します。`（src/content.js:23）が
    出ているはず。

---

### 4-5. ブースト（100 超）

- [ ] **8. ブーストを ON にすると上限が 300 になる（既定は OFF）**
  - 期待: **v0.8 でこの行は YouTube の設定メニューと同じトグルスイッチになりました。**
    ラベル「ブースト」が左、白いつまみのトグルが右。押すとつまみが右へ動き、
    ツールチップが「上限 300%（押すと 100% に戻す）」になる
    （`ytvp-boost__switch` / src/overlay.js:313、`boostVal.textContent` / src/overlay.js:795）。
    **`ON` / `OFF` の文字は画面には出ません**（読み上げのためだけに残し、CSS で画面外へ置いています。
    `ytvp-boost__val` / src/overlay.js:309）。状態は**つまみの位置**で読み取ってください。
    その後 `150` を入力できるようになり、スライダーの上限も 100 → 300 になる
    （`range.max` / src/overlay.js:761）。
    **v0.7 では、ポップアップとプレイヤー内パネルは同じ部品なので、
    どちらで見ても同じ姿になります**（片方だけ 300 にならないなら、それが不具合です）。
    **v0.8 で「100 を超えたら色が変わる」表示はやめました**（`撤回` / SPEC.md:201-205。
    YouTube のプレイヤー UI は白と黒だけで、色は進行バーの赤しか使わないため。
    独自の色を持ち込むこと自体が異物感の主因だった）。
    100 を超えたことは数字そのものが示し、ブーストの ON/OFF は
    トグルのつまみの位置が示します。パネルの文字は全部白です。
  - 動かなかったら: `100` 止まりなら `boostAllowed` が保存できていない（`chrome.storage.local`）。
  - **100 超で実際に音が大きくなるかは誰も確認していません。** ブーストは Web Audio
    （`createMediaElementSource` → `GainNode` → `DynamicsCompressorNode`、src/page.js:60-83）で作りますが、
    このコードは Node で動かせないため stub で代替検査しただけです
    （「契約どおりの stub で足りる」/ test/helpers/wiring.js:99-101）。
    **音が割れる・逆に無音になる・元に戻せない、いずれも起こりうる**と考えてください。
    `createMediaElementSource` は 1 要素につき 1 回しか呼べず、**一度挿した経路は外せません**
    （`WeakMap で保持して使い回す` / src/page.js:57-58。
    OFF にしたときは gain を 1.0 に戻して素通しにするだけ＝`graph.gain.gain.value = 1` / src/page.js:148-152）。
    **無音になったらタブをリロードしてください。**

- [ ] **9. ★ブーストを OFF にしてポップアップを開き直し、もう一度 ON に戻せる**
  - 手順: ブーストを ON → **OFF** → ポップアップを閉じる → もう一度開く → **ON にする**。
  - 期待: 何度でも往復できる。ON/OFF のどちらでもトグルは押せる状態のまま。
  - 動かなかったら: **これは過去に実在したバグの再発です。**
    `boost` と `boostAllowed` を別概念として扱っていた頃、OFF にすると許可ごと消えて
    二度と ON に戻せない一方通行が起きていました（「二度と ON に戻せない一方通行」/ SPEC.md:125-144。実測で発見）。
    現在は「トグルを `boostAllowed` 自身の値で `disabled` にしてはならない」という契約で
    塞いであります（`自分自身の値で自分を押せなくする` / src/overlay.js:303、
    `boostBtn.disabled = state.available === false` / src/overlay.js:798）。
    **再発していたら、契約が実装に反映されていないということなので、必ず issue を立ててください。**
  - オーバーレイ側のブーストボタンでも同じ往復ができるか確認すること
    （契約はポップアップとオーバーレイの両方に適用される。`boostAllowed` / SPEC.md:137-140）。

---

### 4-6. 画面遷移と YouTube Music

- [ ] **10. 別の動画へ移動しても動き続ける（SPA 遷移）**
  - 手順: 動画ページから関連動画をクリックして別の動画へ移動する（リロードしない）。
  - 期待: オーバーレイが出たままで（一度破棄して作り直される。`unmountOverlay` / src/content.js:379-384）、
    ポップアップも引き続き数値を表示する。再バインドの契機は `yt-navigate-finish`（SPEC.md:181）。
  - 動かなかったら: オーバーレイが消えたまま／二重に出る／音量操作が効かない。
    コンソールに `[YTVP] bind 失敗`（src/content.js:499）が出ていないか見る。
  - **`yt-navigate-finish` が現在の YouTube でまだ発火するかは未確認です。**
    YouTube 側の内部イベント名なので、変わっていれば SPA 遷移で**動かない可能性があります**。

- [ ] **11. `music.youtube.com` でも同じように動く**
  - 期待: manifest の対象に含まれるので content script は入る
    （`music.youtube.com` / manifest.json:8-9、`"matches"` / manifest.json:13-16）。
    ポップアップのホスト判定も `music.youtube.com` を通す（`YT_HOSTS` / src/popup.js:34）。
  - 動かなかったら: **YouTube Music に `#movie_player` という id の要素があるかを
    誰も確認していません（未確認）。** 無ければ「プレーヤー未検出」になり、
    オーバーレイは画面右上固定へフォールバックします（`host = doc.body` / src/overlay.js:170-174）。
    音量操作が丸ごと効かない可能性があります。

---

### 4-8. ネイティブボタン（v0.2・SPEC 第9章）

v0.2 で、プレイヤー右下のコントロール群（設定⚙などが並ぶ列）の**左端**に、
現在音量の数値ボタンを差し込むようになりました（生成は src/overlay.js:230-241 `ytvp-native-btn`、
列の先頭への挿入は src/overlay.js:836-837 `insertFirst`。差し込み先を探すのは src/content.js:284-292 の
`findNativeAnchor` で、`#movie_player` 配下の `.ytp-right-controls` だけを見ます）。
**この置き場所は実機で確認済みです**（2026-09-03 時点の `www.youtube.com` に
`.ytp-right-controls` は実在し、ボタンはそこに出ました）。ただし YouTube 側の DOM は
予告なく変わるので、将来も見つかる保証はありません。見つからなければ黙って浮きハンドルに戻ります。

**置き場所は v0.2 のままです。** 純正の 🔊 の隣（`.ytp-left-controls`）へ寄せる案は
2026-09-03 の実機で失敗し、**断念しました**（ボタンは押せるのにパネルが画面に出ない）。
理由と再挑戦の条件は KNOWN_GAPS.md の J 節に書いてあります。**右のままなのは手抜きではなく決定です。**

- [ ] **13. プレイヤー右下、⚙（設定）の並びの左端に、音量の数値ボタンが出る**
  - 期待: `100` のような数値だけのボタン。マウスを乗せると「音量 100%（クリックでパネル）」
    のツールチップ（`nativeBtn.title` / src/overlay.js:781）。
  - **出なかったら: それは故障ではなく設計です。** 差し込み先が見つからないときは
    従来の右上の浮きハンドル（§4-4）へ黙って戻ります（SPEC 9-b。`pickAnchor` / src/overlay.js:122-130）。
    出ない場合は「ボタンが無い＋右上ハンドルは出る」の組で記録してください。
    どちらも出ないなら §4-4 項目6 の失敗として扱う。
  - 起動直後はプレーヤーの DOM が間に合わず、約1秒後に1回だけ差し直しを試みます
    （`ANCHOR_RETRY_MS = 1000` / src/content.js:271、`scheduleAnchorRetry` / src/content.js:358-370）。
    **開いた瞬間に無くても、数秒待ってから判定してください。**

- [ ] **14. ボタンにマウスを乗せる／クリックするとパネルがボタンの上に開き、操作が効く**
  - 期待: 数値の入力欄（その行の右端に ⚙）・スライダー・プリセット pill・ブースト の載ったパネル（中身は §4-4 と同一。
    ネイティブモード専用の操作体系はありません）がボタンの**上側**に開き、音量操作が §4-3 と同様に効く。
    **乗せただけでも開きます**（v0.6。クリックは「固定」の意味に変わりました）。
    **入力欄そのものの確認は §4-9、開閉の作法は §4-11、⚙ は §4-12 で行うので、
    ここでは「パネルが開いて音量操作が効くか」だけを見る。**
    ボタンの数値も操作に追随する。ボタンの上でのホイール（1刻み / Shift+5刻み）も効く
    （`nativeBtn, 'wheel'` / src/overlay.js:672-679）。
  - 動かなかったら: クリックで YouTube 側の反応（再生停止・全画面）が誘発されるようなら
    stopPropagation（`nativeBtn, 'pointerdown'` / src/overlay.js:668-671）が実 DOM で効いていないということなので記録。

- [ ] **15. ⚠️★コントロールバーが自動で隠れたとき、ホバーで開く導線がどう振る舞うか**
  - ⚠️ **この項目が、この版の最初の判断ポイントです。**
  - まず前提（v0.2 から変わらない部分）: ボタンもパネルもコントロール列（`.ytp-right-controls`）の
    中に居るため（`insertFirst` / src/overlay.js:836-837）、**YouTube がコントロールバーを隠すと
    一緒に消えます。これは構造上の帰結でバグではありません。**
  - **新しいのは、v0.6 で開閉をホバーに載せたことです。** YouTube のコントロールバーは
    再生中にマウスを止めると数秒で自動的に隠れます。そのときブラウザが `mouseleave` /
    `pointerleave` を飛ばすかどうかで体感がまるごと変わります
    （離脱を拾うのは `bindHover` / src/overlay.js:649-655、閉じるのは猶予つきで
    `scheduleClose` / src/overlay.js:609-619、猶予の長さは `CLOSE_GRACE_MS` / src/overlay.js:10）。
  - **この相互作用だけは、いまも偽 DOM では検査できません。** ホバーの導線そのものは
    2026-09-03 の後半に自動テストを足しました（継ぎ目8。§4-10 末尾）が、それは
    「`mouseenter` が来たら開く」までしか見ていません。**バーが自動で隠れるときに
    ブラウザが `mouseleave` を飛ばすかどうかは Chrome の挙動**で、偽 DOM には
    再現するものがありません。ここが実機でしか分からない部分です。
  - **確認してほしいこと**（そのまま書いてください）:
    ① バーが隠れたときパネルも消えるか
    ② マウスを動かしてバーが戻ったとき、パネルは開いたままか・閉じているか
    ③ **クリックで固定した状態（§4-11 項目30）でも**バーごと消えるか
    ④ 固定したまま消えて戻ったとき、固定は生きているか
  - **感想がほしい**: この挙動を日常使用で許容できるか。許容できないなら次版の設計判断
    （パネルだけコントロール列の外へ出す等）に使うので、率直に書いてください。

- [ ] **16. 音量を 3 桁（150 など。ブースト ON が必要）にしたとき、ボタンが極端に幅を取らない**
  - 期待: ボタンは最低幅 40px・文字は等幅数字で、3桁では内容の分だけ広がります
    （幅の固定はしていない。`min-width: 40px` / src/overlay.css:118-126、
    `.ytvp-native-btn__value` / src/overlay.css:152-155）。**YouTube 純正の ⚙（設定）**などの
    並びが不自然に押し出されないかを見てください（パネルの中の ⚙ は別物です。§4-12）。
    **実際のプレーヤーでどう見えるかは未確認です。**
    **v0.8 から、100 超でも数値の色は変わりません**（警告色 amber は YouTube のプレーヤー UI に
    無い色で、一番浮いていたため撤回）。クラスは付き続けますが見た目は持ちません
    （`ytvp-native-btn--boost` / src/overlay.js:778-780）。

- [ ] **17. `music.youtube.com` ではこのボタンが出ない（出ないのが正しい）**
  - 期待: ネイティブ統合の対象は `www.youtube.com` だけ（`NATIVE_HOSTS` / src/content.js:266）。
    music では従来の浮きハンドル（§4-6 項目11 のとおり）。
  - もし music にもボタンが出たら、ホスト判定（`isNativeHost` / src/content.js:273-280）が実環境で
    効いていないということなので記録してください。

---

### 4-9. パネルの数値入力（v0.3・SPEC 第10章と追補）

**ここがこの製品の出発点です。** 元々の要望は「ゲージじゃなく数値で入れたい」でした。
v0.2 まではパネルの大きな数字は**読むだけ**で、要望に応えていませんでした。
v0.3 で readout それ自体を入力欄にしてあります
（`var valueEl = el(doc, 'input', 'ytvp-value');` / src/overlay.js:262-271）。
ポップアップ側の数値入力（§4-3 項目4）は v0.2 から在るもので、別物です。**ここで見るのは
プレイヤーの中のパネルです。**

⚠️ **過去に2回、実機で「数値が打てない」で落ちています。**
原因は、YouTube のコントロールバーがドラッグでのシークや選択を防ぐために `mousedown` で
`preventDefault()` している可能性で、**mousedown の既定動作＝フォーカス移動そのもの**なので、
入力欄をクリックしてもカーソルが入りません。今回は3層の対策を入れてあります
（①パネル内の `mousedown` / `pointerdown` の伝播を止める: `rootEl, 'mousedown'` / src/overlay.js:714-715
②入力欄の上で明示的に `focus()` を呼ぶ: `focusOnPointer` / src/overlay.js:540-546
③CSS で選択を text に戻す: `user-select: text` / src/overlay.css:247-251）。
**それでも実機で直った証明はありません。** 3度目も落ちる可能性があります。
落ちたら気を遣わずにそう書いてください。**直っていないという事実のほうが価値があります。**

v0.6 で足した ⚙ の中のプリセット欄（§4-12）にも同じ3層を当ててあります
（`pointerFocus` / src/overlay.js:408-412）。音量の入力欄が打てて**プリセット欄だけ打てない**、
あるいはその逆が起きたら、原因の切り分けに効くので必ず組で書いてください。

- [ ] **18. ★パネルを「クリックで」開いた瞬間、大きな数字が全選択された状態でフォーカスされている**
  - 手順: ネイティブボタン（§4-8 項目13）か、出ていなければ浮きハンドル（§4-4 項目6）を
    **1回クリックする**。
  - **v0.6 の注意**: マウスを乗せただけでもパネルは開きますが、**そのときは意図的に
    フォーカスを取りません**（マウスが通っただけで YouTube のショートカットを奪わないため。
    理由と判定手順は §4-11 項目31）。この項目は**クリックで開いた場合**の期待値です。
  - 期待: パネルが開くのと同時に大きな数字にカーソルが入り、**数字が反転（全選択）している**。
    どこもクリックせずに数字を打つと、既存の値が置き換わる
    （`focusInput` / src/overlay.js:511-515、`valueEl.select` / src/overlay.js:514）。
  - **これが落ちたら項目19〜22 は全部落ちます。** そのとき、
    「カーソルがまったく入らない」のか「カーソルは入るが全選択されていない」のかを分けて書いてください。
    前者は上の3層が効いていない、後者は `select()` だけが効いていない、で原因が違います。

- [ ] **19. ★数字を打って Enter → ゲージに触らずに音量が変わる**
  - 手順: 項目18 の直後に `37` と打って Enter。
  - 期待: 大きな数字が `37` になり、スライダーも 37 の位置へ動き、耳で聞こえる音量も変わる。
    **スライダー（ゲージ）は一度も触らないこと。** 触ったらこの項目は検証になりません。
  - 解釈は共有ロジックに一本化してあるので、`087` も `87%` も全角の `８７` も同じ 87 になります
    （`parseVolumeInput` / src/overlay.js:90-99、`INTEGER_RE` / src/lib/volume.js:44-66）。
    確定後は打った文字が正規形（`87`）に揃います。これは正常です。

- [ ] **20. `abc` と空欄の Enter は適用されない／Esc と欄外クリックで取り消せる**
  - 手順: ①`abc` と打って Enter ②全部消して空のまま Enter ③数字を打って Esc
    ④数字を打ってパネルの外（動画の上など）をクリック。
  - 期待: ①〜④のどれでも**音量は変わらず**、表示が現在値へ戻る
    （解釈できなければ適用しない: `if (parsed === null) { revertInput(); return false; }` / src/overlay.js:492-502、
    Esc: `revertInput` / src/overlay.js:554-559、blur: `blur は適用しない` / src/overlay.js:524-528）。
  - **v0.6 では、③の Esc と ④の欄外クリックはパネルも閉じます（固定の解除。§4-11 項目30）。
    閉じること自体は設計どおりです。** 判定は「閉じる前後で音量が変わっていないか」で行ってください。
  - ③で **Esc が全画面まで抜けてしまう**ようなら、それは別の問題なので書いてください（未確認）。

- [ ] **21. ★入力中に数字キーを打っても動画がシークしない**
  - 手順: 動画を再生したまま項目18〜19 を行い、`5` `0` などを打つ。
  - 期待: 再生位置が飛ばない。YouTube は数字キー＝シーク・`k`＝停止・`f`＝全画面をページ全体で拾うため、
    パネル内では keydown / keyup / keypress の3種とも伝播を止めてあります
    （`rootEl, 'keydown'` / src/overlay.js:717-719）。
  - **止まらない可能性があります（既知の穴）。** YouTube が `document` の**キャプチャ段**で
    キーを拾っていると `stopPropagation` では届きません（KNOWN_GAPS.md の I 節 28番。
    そこでは「§4-8 項目18」と書かれていますが、節を分けた結果この項目のことです）。
    飛んだら「入力中に数字キーでシークした」と書いてください。**あなたの操作ミスではありません。**

- [ ] **22. 打っている最中に、外からの更新で数字が消えない**
  - 手順: 入力欄に `12` まで打った状態で 2〜3 秒放置する（その間に content からの状態更新が届く）。
  - 期待: 打った `12` が消えない。編集中は入力欄だけ再描画を飛ばします
    （`if (!isEditing())` / src/overlay.js:753-756、`isEditing` / src/overlay.js:476-480）。
  - ハンドル・ネイティブボタンの数字や pill の塗りは編集中も更新されます。それが正常です。

- [ ] **23. （未確認・分かる範囲で）日本語入力（IME）が ON のときの Enter**
  - **これは誰も確かめていません。期待値も決めていません。**
    変換確定の Enter と適用の Enter が干渉しうるのに、キー処理は `isComposing` を見ていません
    （`k === 'Enter'` / src/overlay.js:548-560。2026-09-03 に `grep -rn isComposing src/` が0件で実測）。
  - **起きたことをそのまま書いてください**（変換が確定すると同時に適用された／1回目の Enter は
    変換確定だけで済んだ／何も起きなかった、など）。

---

### 4-10. プリセットの編集（v0.5・SPEC 第12章）

「デフォルトはこれでいいから、ある程度自由に変えれるようにしたい」という要望への対応。

**v0.5 の「編集欄はポップアップだけ」という前提は、v0.6 → v0.7 で完全に入れ替わりました。**
設定の家はプレイヤー内のパネルの ⚙ ひとつです（§4-12）。
拡張のアイコンは Chrome の既定ではツールバーに出ず、パズルピースの中に隠れるため、
ポップアップにしか無い設定は多くの人に届かない、というのが理由です。

**v0.7 の変更点（重要）**: ポップアップは自前の編集フォームを**持たなくなりました**。
ポップアップもプレイヤー内パネルと同じ部品を描くので、**編集欄は両方とも ⚙ の中**にあります。
「片方にしか無い機能」が生まれる余地を無くすための変更です。
したがって **この §4-10 で見るのは「ポップアップの ⚙ から編集したとき」** になります。
操作そのものは §4-12 項目34 と同じで、違うのは開いている窓だけです。

- [ ] **24. ポップアップの ⚙ から編集して適用すると、ポップアップの pill が変わる**
  - 手順: ポップアップを開く → ⚙ を押す → 出てきた1行の欄に `10 25 40 60` と入れて
    「適用」を押す（Enter でも可）。
  - 期待: pill が `10` `25` `40` `60` に変わる
    （`ytvp-preset-apply` / src/overlay.js:368、`applyPresets` / src/overlay.js:450-454）。
  - 区切りはカンマでも空白でも構いません。数字以外はすべて区切りとして扱い、解釈できない断片は
    黙って捨てます（`readPresetInput` / src/overlay.js:436-448）。
  - **⚙ がポップアップに見当たらないなら**、それは配線の問題です（§4-12 の項目33 と同じ切り分け）。

- [ ] **25. ★プレイヤー内パネルの pill にも反映される（パネルは一度閉じて開き直す）**
  - 手順: 項目24 のあと、YouTube のページ側でパネルを開く。**すでに開いていたら開き直す。**
  - 期待: パネルの pill も `10` `25` `40` `60` になっている。
  - **開いていたパネルがいったん閉じます。これは故障ではなく設計です。**
    パネルの pill は mount のときの options で決まるので、反映するには作り直すしかありません
    （`remountOverlay` / src/content.js:228-236、`needsRemount` / src/content.js:241-256）。
    作り直すのは**正規化した結果が実際に変わったときだけ**です
    （`presetsChanged` / src/content.js:214-220）。音量・ブーストの操作では
    作り直しません（そのたびにパネルが閉じてしまうため）。
  - **両方の pill が変わったか**を必ず組で記録してください。片方だけ変わるのが、この機能で
    最も起こりそうな壊れ方です。

- [ ] **26. 空欄で適用すると 15 30 50 70 100 に戻る**
  - 手順: 欄を空にして「適用」。
  - 期待: pill が既定の5個に戻る。リセットボタンはありません
    （`DEFAULT_PRESETS` / src/lib/volume.js:7、
    `if (out.length === 0) { return DEFAULT_PRESETS.slice(); }` / src/lib/volume.js:68-81）。
    欄の見出しにも同じ注記が出ています
    （「プリセット（空白区切り・空欄で既定に戻す）」/ src/overlay.js:356-358）。

- [ ] **27. 打った値と違う形で残ることがある（それは正規化の結果です）**
  - 手順: わざと `40 10 10 999` のように順不同・重複・範囲外を混ぜて適用する。
  - 期待（ブースト OFF のとき）: `10` `40` `100` の3個になる。昇順に並べ替え、重複を捨て、
    上限（OFF なら 100 / ON なら 300）へ丸め、最大8個で切ります
    （`out.sort` / src/lib/volume.js:79、`out.slice(0, 8)` / src/lib/volume.js:80、
    `clampVolume` / src/lib/volume.js:75）。
  - 適用後の欄には**実際に保存された値**が書き戻ります
    （パネルが作り直され、`presetInput.value = presets.join(' ')` / src/overlay.js:363。
    ⚙ を開き直したときにも同じ値を入れ直します / src/overlay.js:429）。
    打った文字がそのまま残らないのは異常ではありません。
  - 300 まで入れたいときは先にブーストを ON にしてください（OFF のままだと 100 に丸められます）。

- [ ] **28. オフライン（YouTube 以外のタブ）ではパネルごと消える**
  - **v0.7 で挙動が変わりました。** 編集欄を灰色にするのではなく、
    **パネルを丸ごと畳んで案内文だけにします**（`goOffline` / src/popup.js:190-201）。
  - 期待: YouTube 以外のタブでポップアップを開くと「YouTube を開いてください」の1行だけで、
    数値欄も pill も ⚙ も出ない。
  - **理由**（`disabled` にしなかった理由）: disabled で塞げるのは overlay が塞ぐ部品
    （数値・スライダー・ブースト）だけで、プリセットの pill は塞げず**押せてしまう**。
    押せるのに何も起きないボタンを残さない、という判断です。
  - 動かなかったら: 押せる pill が残っていた／欄が出たまま操作できてしまう、を書いてください。

**この節から先について、機械の検査が持っているもの・持っていないもの（不利な事実も先に出す）**:

- **2026-09-03 の後半に、§4-11・§4-12 の挙動を偽 DOM で検査する回帰を足しました。**
  それ以前は「`test/` の中に `mouseenter` / `hover` / `pinned` / `gear` / `firstRunHint` を
  含むファイルが1つも無い」状態で、実装だけが先に進んでいました。いまは
  `test/integration.test.js` の「継ぎ目8」に次の検査があります:
  ⚙ を出す条件（`bridge.setSettings` があるときだけ）／⚙ の「適用」が保存へ流れること／
  ホバーで開き離れると閉じること／タッチでは開かないこと／クリックで固定されること／
  **ホバーで開いたときは入力欄を奪わず、クリックのときだけ全選択すること**／
  編集中は閉じないこと（音量欄とプリセット欄の両方）／初回ヒントが1回だけ出ること。
  **どれも「壊した検体で本当に赤くなる」ところまで実測してあります**
  （`tools/tests/run_scanner_regress.sh` の「継ぎ目8」の節。検体は
  `test/fixtures/integration/patch_overlay_no_hover.js` 等）。
- **それでも、これは偽 DOM の上での検査です。** 実ブラウザも実 YouTube も触っていません。
  とくに **YouTube のコントロールバーが自動で隠れることとの相性（§4-8 項目15）は、
  偽 DOM では原理的に検査できません。** バーが消えるときに `mouseleave` が飛ぶかどうかは
  Chrome の挙動であって、こちらのコードの問題ではないからです。
- **§4-9（パネルの数値入力）の「実機で打てるか」は依然として未検査です。**
  過去2回落ちている 3層の対策（伝播停止・明示 focus・CSS）は、どれも
  「YouTube 側が `mousedown` で `preventDefault()` する」という**外部の挙動**への対策なので、
  偽 DOM には再現するものがありません。**3度目も落ちる可能性があります。**
- SPEC 10-b・12-b が要求している検体は、この回帰で一部満たしましたが、
  ホバー／固定／⚙／初回ヒントは**そもそも SPEC に章がありません**（実装が仕様より先に出ている）。
  検査はあるが仕様書が無い、という順番の逆転は残っています。
- したがって `verify.sh` が緑でも、意味するのは「偽 DOM の上で意図どおり動く」までです。
  **実機で動く根拠にはなりません。**

---

### 4-11. パネルの開き方（v0.6・ホバーで開く／クリックで固定）

**狙いは「新しく覚えることを増やさない」ことです。** YouTube 純正の 🔊 は
「乗せると伸びる・外れると畳まれる」。同じ作法に寄せてあります
（`同じ作法にすると、利用者が新しく覚えることが無くなる` / src/overlay.js:190）。

⚠️ **この節と §4-8 項目15 は同じものの表と裏です。** YouTube のコントロールバーは数秒で
自動的に隠れます。**その自動非表示とホバーの相性だけは、偽 DOM では検査できません**
（導線そのものは継ぎ目8 が検査しています。§4-10 末尾）。
**先に §4-8 項目15 を読んで、そこが最初の判断ポイントであることを頭に入れてから**この節へ来てください。

- [ ] **29. ボタン（またはハンドル）にマウスを乗せると開く／両方から離れると少し遅れて閉じる**
  - 手順: クリックせずにボタンの上へマウスを乗せる → 開いたら、ボタンからパネルへマウスを移す
    → パネルの外へ抜く。
  - 期待: ① 乗せただけで開く ② ボタンとパネルの間を渡っている途中で閉じない
    ③ 両方から離れると、すこし遅れて閉じる
    （ボタンとパネルの両方にホバーを張っている `bindHover` / src/overlay.js:649-659、
    閉じるまでの猶予は `CLOSE_GRACE_MS` / src/overlay.js:10）。
  - **タッチでは開きません（設計）。** ポインタ種別が touch のときはホバー経路を丸ごと無視し、
    クリックだけが残ります（`isTouchPointer` / src/overlay.js:623-625）。
  - 動かなかったら:「乗せても開かない」「渡る途中で消える」「離れても閉じない」を**区別して**
    書いてください。原因が別です。

- [ ] **30. クリックすると固定される（離れても閉じない）／再クリック・Esc・パネル外クリックで解除**
  - 手順: ① ボタンをクリックしてからマウスを大きく離す ② もう一度ボタンをクリックする
    ③ 開き直して Esc ④ 開き直して動画の上（パネルの外）をクリック。
  - 期待: ① 離しても開いたまま ②〜④ のどれでも閉じる
    （固定は `pinned = true` / src/overlay.js:584、解除は `unpinAndClose` / src/overlay.js:737-744、
    Esc の経路は src/overlay.js:724-727 `unpinAndClose`、
    外クリックの経路は src/overlay.js:732-735 `unpinAndClose`）。
  - ③ で **Esc が全画面まで抜ける**、④ で **再生が止まる／全画面に入る**といった巻き添えが
    起きたら、それも書いてください（未確認）。

- [ ] **31. ★ホバーで開いただけのときは入力欄にフォーカスが入らない／クリックのときだけ全選択される**
  - **なぜ分けてあるか**: マウスが通り過ぎただけで入力欄にカーソルが入ると、そこから先の
    `k`（停止）・`f`（全画面）・数字キー（シーク）といった YouTube のショートカットが全部死にます。
    **通っただけで操作系を奪わない**のが狙いです。
  - 手順: ① 乗せて開いただけの状態で `k` を押す ② いったんマウスを外し、今度はクリックで開く。
  - 期待: ① 動画が停止する（＝入力欄にフォーカスが入っていない証拠）
    ② クリックのときだけ大きな数字にカーソルが入り、**数字が反転（全選択）している**
    （フォーカスと全選択を呼ぶのはクリックの経路だけ。`focusInput(true)` / src/overlay.js:591、
    全選択の実体は `valueEl.select` / src/overlay.js:514）。
  - **① で動画が止まらなかったら、ホバーだけでフォーカスを奪っています。** 必ず不具合として記録してください。
    ② が落ちた場合の切り分けは §4-9 項目18 と同じです。

- [ ] **32. 入力欄を編集している間は、マウスが離れてもパネルが閉じない**
  - 手順: クリックで開いて `12` まで打ち、**そのままマウスをパネルの外へ出して数秒待つ**。
  - 期待: パネルは閉じず、打った `12` も残っている
    （編集中は閉じずに猶予を取り直す。`isTypingAnywhere` / src/overlay.js:598-603）。
  - 同じことを ⚙ の中のプリセット欄でも試してください（§4-12 項目33 のあと）。
    **音量欄では閉じないのにプリセット欄では閉じる**、という壊れ方がありえます。

---

### 4-12. ⚙ 設定（パネルの中でプリセットを編集）と初回ヒント（v0.6）

**狙いは「設定の家を1つにする」ことです。** 拡張のアイコンは Chrome の既定ではツールバーに
出ず、パズルピースの中に隠れます。そこにしか無い設定は多くの人に届かないので、プレイヤー内の
パネルを唯一の家にし、普段は隠して ⚙ で開く形にしてあります
（`プレイヤー内パネルを` / src/overlay.js:329-330）。

**⚙ と初回ヒントは、いまは content 側とつながっています（2026-09-03・実測で確認）。**
前の版のこの位置には「この2つは出ないほうが正常」という趣旨の警告がありましたが、
その後 `src/content.js` 側の実装が入って前提が変わったので**削除しました**。
いまの事実は次のとおりです。

- overlay は `bridge.setSettings` を持つ相手にだけ ⚙ を出します
  （`canSettings` / src/overlay.js:333、生成は `if (canSettings)` / src/overlay.js:340 の中だけ）。
- content が渡す bridge には `setSettings` と `markHintShown` が**あります**
  （`markHintShown: function ()` / src/content.js:317）。
- mount へ渡す options にも `firstRunHint` が**あります**
  （`firstRunHint: wantHint` / src/content.js:339。overlay 側の受けは
  `firstRunHint` / src/overlay.js:402）。

**したがって項目33〜35 は「出るのが期待値」です。出なかったらそれは不具合であり、
故障として記録してください**（前の版と逆になっています。読み替えに注意）。
偽 DOM の上では、⚙ が保存へ届くこと・初回ヒントが1回だけ出ることを
`test/integration.test.js` の継ぎ目8 が検査しています（§4-10 末尾を参照）。
**それでも実機で出る保証にはなりません。**

- [ ] **33. パネルの中に ⚙ があり、押すと編集欄がその場で開く（別の場所へ飛ばない）**
  - 手順: パネルを開く（§4-11）→ ⚙ を押す。**⚙ はパネルの最上段、大きな数値の行の右端**にあります
    （`readout.appendChild(gearBtn)` / src/overlay.js:353）。
  - 期待: パネルの中に「プリセット（空白区切り・空欄で既定に戻す）」の1行の欄と「適用」が現れる。
    **開く場所は下端なので、押した ⚙ と離れています。** 対応が分かるよう、開いている間は
    ⚙ と編集欄に同じ地色が付きます（`.ytvp-panel--settings` / src/overlay.css:512-518、
    `ytvp-gear--on` / src/overlay.js:419）。**この色の対応が付いているかも見てください。**
    **ポップアップも新しいタブも開きません。** 欄には今保存されている値が入っている
    （`ytvp-preset-input` / src/overlay.js:361、開くたびに入れ直す
    `presetInput.value = presets.join(' ')` / src/overlay.js:429）。
  - もう一度 ⚙ を押すと畳まれる（`setSettingsOpen(!settingsOpen)` / src/overlay.js:375）。
    パネルを閉じると ⚙ も畳まれた状態に戻る
    （`if (!open) { setSettingsOpen(false); }` / src/overlay.js:568）。
  - **⚙ が見当たらなかったら**: これは不具合です。「パネルは出るが ⚙ が無い」の形で
    記録してください（パネルごと出ていないのなら §4-4 項目6 / §4-8 項目13 の失敗です）。

- [ ] **34. 欄を書き換えて「適用」→ pill が変わる／空欄で「適用」＝既定に戻る**
  - 手順: ① `10 25 40 60` と入れて「適用」（Enter でも可）② 欄を空にして「適用」。
  - 期待: ① pill がその4個になる ② 既定に戻る。リセットボタンはありません。
    空欄は空の配列として送られ、既定へ戻すのは共有ロジック側の既存の挙動です
    （`readPresetInput()` / src/overlay.js:453、
    `if (out.length === 0) { return DEFAULT_PRESETS.slice(); }` / src/lib/volume.js:68-81）。
  - **パネルがいったん閉じて開き直るはずです。これは故障ではなく設計です**（理由は §4-10 項目25）。
  - 打った値と違う形で残ることがあります（昇順・重複除去・丸め・上限個数）。§4-10 項目27 と同じです。
  - ポップアップ側の pill にも同じ値が出ているかを**組で**確認してください。片方だけ変わるのが、
    この機能で最も起こりそうな壊れ方です（§4-10 項目25 と同じ理由）。

- [ ] **35. インストール後に最初に YouTube を開いたときだけ、短いヒントが数秒出る**
  - 期待: パネルの近くに「音量を数値で。クリックで開く」が出て、数秒で消える
    （文言は `音量を数値で。クリックで開く` / src/overlay.js:403、消えるまでの時間は
    `HINT_MS` / src/overlay.js:12）。ヒントはクリックの邪魔をしません
    （`pointer-events: none` / src/overlay.css:588）。
  - **2回目以降は出ないのが期待値です。** 既読を覚えるのは overlay ではなく content 側の役目で、
    overlay は渡された値に従い、出したことを知らせるだけです
    （`markHintShown` / src/overlay.js:851）。
  - **出なかったら**: 不具合として記録してください（既読が既に立っている可能性もあるので、
    下の「再現」を試してから判断すると確実です）。
    **毎回出るなら**、既読が保存されていないということなので、これも記録してください。
  - 既読は `chrome.storage.local` の `hintShown` に入ります
    （キー名は `KEY_HINT = 'hintShown'` / src/content.js:31、保存は
    `storageSet(KEY_HINT, true)` / src/content.js:206）。
  - 再現には**拡張を削除してから入れ直す**のが確実です（削除で `chrome.storage.local` ごと消えます。§8）。
    無理をせず「試せなかった」で構いません。

---

## 5. うまくいかないときの読み方（3か所を見る）

### 5-1. `chrome://extensions` の「エラー」ボタン
当該拡張のカードに赤い「エラー」ボタンが出ていたら押す。
manifest の書式エラー、参照先ファイルの不足、content script の読み込み時例外がここに出る。

### 5-2. ポップアップ自身の DevTools
ポップアップを開いた状態で**その上を右クリック →「検証」**。
- 正常時は**何も出ないのが期待値です**（`src/popup.js` に通常時のログ出力は1行も無い）。
  「ログが出ないから壊れている」とは判断しないでください。
- 見るべきは赤いエラー。とくに `chrome.tabs` 関連が出ていれば §4 項目3 の失敗原因になります。
- Console で `await chrome.tabs.query({active:true,currentWindow:true})` を実行すると、
  **`url` が返っているかどうか**を直接確認できます。§4 項目3 が落ちたときはこれを見てください。

### 5-3. YouTube ページの DevTools コンソール
YouTube のページ上で F12 → Console。本拡張のログは**すべて `[YTVP]` で始まります**。
出うる文字列の**全数と現在の行番号**は、これで出ます（下の表を数えず、この出力を読んでください）:

```
grep -n "\[YTVP\]" src/content.js
```

意味はこの表で引く（**この表に行番号は書きません**。v0.2 の実装で src/content.js の行番号が
一斉にずれ、この表の7引用が全部腐ったのが理由です。文字列は滅多に変わらないが行番号は
実装のたびに動く。位置は上の grep で毎回実測すること）:

| 文字列 | 意味 |
|---|---|
| `[YTVP] volume.js が読み込まれていません。中止します。` | 共有ロジック不在。以降すべて動かない |
| `[YTVP] オーバーレイ未実装のためスキップします（本体機能は動きます）。` | overlay.js が読めていない |
| `[YTVP] オーバーレイの mount に失敗しました` | overlay の生成で例外 |
| `[YTVP] subscriber error` | 状態通知の購読側で例外 |
| `[YTVP] overlay update error` | オーバーレイの再描画で例外 |
| `[YTVP] ready 同期に失敗` | page.js との初期同期に失敗 |
| `[YTVP] bind 失敗` | 起動 / SPA 遷移時の再バインドに失敗 |

表に無い `[YTVP]` 行が grep に出たら、この表が腐っています（表を直して使ってください）。

`src/page.js` にはログ出力がありません。MAIN world 側の不調は
「応答が来ない」＝ 2 秒でタイムアウト（定数は `CMD_TIMEOUT_MS = 2000` / src/content.js:32、
待ち受けの破棄は `delete pending[id]` / src/content.js:70-76）という形でしか現れません。
**沈黙は正常とは限らない**ことに注意してください。

---

## 6. 自分で検査を回す方法

シェルで、リポジトリの直下に移動して:

```
cd <clone した場所>/yt-volume-precise
./verify.sh ; echo "exit=$?"
```

`exit=0` が全緑。**最終行**に `pass=N fail=0` の形で件数が出ます（`----` の次の行）。
**N（項目の総数）は検査が足されれば増えます。数を覚えないでください。見るのは `fail=0` と `exit=0` です。**

**緑が保証する項目の一覧は、この文書には書きません。** 手で写した一覧は必ず腐るからです
（実際にこの節は、書いた当日のうちに2回腐りました。経緯は下の記録）。
**現物は、実行したときの出力そのものです。** これを打ってください:

```
bash verify.sh | grep -E '^\[(OK|NG)\] '
```

各行の `[OK]` / `[NG]` の後ろに、その項目が何を検査しているかが日本語で書いてあります。
それが唯一の一覧です。

⚠️ **この節が2回腐った記録**（消さずに残す。同じ書き方をしないための教材）:
1. 当初この節は `grep -n '^res ' verify.sh` を「実物の出し方」として示していた。
   **2026-09-02 の commit `5a04881` 時点で、これは13項目中12項目しか返さなかった**
   （通信スキャンの判定行だけインデントされていて `^res ` に当たらない）。
   落ちていたのは README の約束3「外部通信を持たない」を支える唯一の機構だった。独立検証の実測で発見。
2. その訂正（commit `6bbb062`）は「`grep -c '^res '` は 12」という**その時点の数字**を書いた。
   **19分後の commit `0fc8d02` が `verify.sh` の構造を変え、同じコマンドは 0 を返すようになった。**
   訂正コミットが、同じバッチの次のコミットに嘘にされた。これも独立検証の実測で発見。

教訓：**ソースを間接的に数える指示・その時点の数字の断定は、書いた瞬間から腐り始める。**
実行した出力を読ませること。数字を書くなら、この記録のように commit を添えて過去形で書くこと。

**緑が保証しないこと（重要）**:
- **`verify.sh` が緑でも、ブラウザで動く保証にはなりません。** 検査は Node 上の
  偽 DOM・偽 `chrome` に対して行われており、実ブラウザも実 YouTube も一切触っていません
  （`verify.sh` は node と bash だけで完結していて、ブラウザを起動する仕掛け
  ―― puppeteer / playwright の類 ―― を一行も持ちません。冒頭でもそう宣言しています）。
- とくに `src/page.js`（`movie_player` と Web Audio）は Node で動かないため、
  結合テストでは stub に置き換わっています（test/helpers/wiring.js:99-103 の `attachFakePlayer`）。
  **音が実際に鳴るかどうかを検査している項目は1つもありません。**
- YouTube 側の DOM やイベント名が変わったことも検知できません。
- 実際のネットワーク通信も見ていません（コードの静的検査のみ）。

**この文書の §4 が、その空白を人手で埋めるためのものです。**

---

## 7. 権限について（この拡張の存在理由）

manifest が要求するものはこれだけです:

- `permissions`: `storage` のみ（`"permissions": ["storage"]` / manifest.json:6）
- `host_permissions`: `*://www.youtube.com/*` と `*://music.youtube.com/*` の2つのみ（`"host_permissions"` / manifest.json:7-10）
- 外部通信のコードはゼロ（`verify.sh` の通信スキャン `tools/scan_network.sh` が機械検査。
  ただし静的検査の限界は §6 と §1 に書いたとおりで、証明ではありません）

**権限画面に何が出るはずか**:
- 出るべきは「**www.youtube.com と music.youtube.com のサイトのデータの読み取りと変更**」に相当する
  1件だけです。`storage` はユーザー向けの警告文を持たないので、表示されないのが正常です。
- ただし「パッケージ化されていない拡張機能を読み込む」経路では、**インストール時の権限確認
  ダイアログが出ないことがあります**（ストア経由と挙動が違う）。出なくても異常ではありません。
  確実に確認したいときは `chrome://extensions` → 当該カードの「**詳細**」→「**サイトへのアクセス**」を見てください。
- **「すべてのウェブサイト上の全データの読み取りと変更」「閲覧履歴の読み取り」が出たら、それは異常です。**
  manifest が想定と違うということなので、**その場で削除**して、issue を立ててください。
  この拡張の存在理由そのものが崩れます。
- 表示文言は Chrome のバージョンで変わります。**上の日本語の文言は逐語では未確認**なので、
  「YouTube 以外のサイトが含まれていないか」を基準に読んでください。

**同梱物についての事実**:
`test/fixtures/` には検査器（`tools/scan_network.sh` 等）を試すための検体があり、
`bad_` で始まるファイル（例: `bad_net.js` / `bad_net_eval.js` / `bad_net_ws.js`。本数は増減します）には
`fetch` / `eval` / `WebSocket` / 外部 URL が**意図的に**書かれています。
これらは manifest から参照されていないため Chrome は読み込まず**実行されません**が、
フォルダ読み込みでも ZIP 化でも**ディスク上・ZIP 内には同梱されます**。

---

## 8. 更新・削除

**コードを変えたあと**:
`chrome://extensions` の当該カードにある**更新（円形の矢印）ボタン**を押す。
その後 **YouTube のタブをリロード**する（content script は再注入されないため。§3 手順6 と同じ理由）。
ページ全体の再読み込みで足りない場合は、カードのトグルを OFF → ON も試す。

**削除**:
`chrome://extensions` の当該カードの「**削除**」ボタン。
`chrome.storage.local` に保存した設定（プリセット・ブースト可否）も
拡張と一緒に消えます。リポジトリのファイルは消えません。

---

## 9. 既知の未確認事項（正直に）

1. **実機での動作は全体が未確認。** 本文書のチェックリストは全項目が「まだ誰も確かめていない」状態。
2. **`chrome.tabs.query` が `tabs` 権限なしで `url` を返すという前提が未確認**（§4 項目3）。
   ここが外れると拡張はまるごと使えません。最大の設計リスク。
3. **`src/page.js`（Web Audio によるブースト）の実挙動が未確認。** Node で動かないため stub で
   代替検査しており、**100 超のブーストが実際にどう鳴るかは誰も確かめていない**（音割れ・無音の可能性）。
4. **manifest にアイコンが未設定**（`icons` キーが無い）。ツールバーに仮アイコンが出る。
   ストア提出時には必要だが、いまは不要。
5. **Chrome ウェブストアには提出していない。** 現状の入手方法は、このリポジトリを clone して
   「パッケージ化されていない拡張機能を読み込む」だけである。
6. `music.youtube.com` に `#movie_player` があるかが未確認（§4 項目11）。
7. `yt-navigate-finish` が現在も発火するかが未確認（§4 項目10）。
8. `\\wsl$\` 形式のパスから未パッケージ拡張を読み込めるかが未確認（§2-1）。
9. Edge での動作は未確認（Chromium 系なので動く見込み、というだけ）。Firefox は未対応。
10. 権限画面の逐語の文言が未確認（§7）。
11. **「内容での引用」（行番号を伴わない実装説明）を検査する機構は無いままです。**
    `ファイル名:行番号` の引用のほうは 2026-09-03 にアンカー語を全数付け直したので、
    行がずれれば `verify.sh` の項目12 が赤くなります。ただしアンカー語は**字面の一致しか見ません**し、
    「そこを引くのが適切か」も見ません。行番号を持たない記述はそもそも検査の対象外です。
12. **v0.2 のネイティブ統合（§4-8）は全体が未確認。** 現行の YouTube に `.ytp-right-controls`
    が存在するかも未確認で、無ければ浮きハンドルへ戻る設計ですが、その「戻り」が実機で
    設計どおり働くかも未確認です。
13. **v0.3 のパネル数値入力（§4-9）は全体が未確認。しかも過去2回、実機で「数値が打てない」で
    落ちている。** 3層の対策を入れてあるが、**実機で直った証明は無い。**
    IME 中の Enter は未確認（`isComposing` を見ていない）。入力中の数字キーでシークしないかも未確認
    （YouTube がキャプチャ段でキーを拾っていれば止まらない。KNOWN_GAPS.md の I 節）。
14. **v0.5 のプリセット編集（§4-10）は全体が未確認。** ポップアップの pill と
    プレイヤー内パネルの pill の**両方**に反映されるかを実機で見た者は居ない。
15. **§4-11・§4-12 の自動テストは 2026-09-03 に足した。§4-9 の「実機で打てるか」は依然として未検査。**
    継ぎ目8（`test/integration.test.js`）が、⚙ の生成条件・⚙ からの保存・ホバーでの開閉・
    タッチ除外・クリックでの固定・ホバー時にフォーカスを奪わないこと・編集中は閉じないこと・
    初回ヒントが1回だけ出ることを偽 DOM で検査し、壊した検体で赤くなることまで実測してある。
    残る穴は2つ:（a）§4-9 の3層の対策は YouTube 側の `preventDefault()` への対策なので
    偽 DOM には再現するものが無い（b）SPEC にホバー／固定／⚙／初回ヒントの章が無く、
    **検査はあるが仕様書が無い**という順番の逆転が残っている。
16. **ネイティブボタンの置き場所は v0.2 のまま（右コントロールの先頭）。**
    左（純正音量の隣）への移動は 2026-09-03 の実機で失敗し**断念済み**（KNOWN_GAPS.md の J 節）。
    未確認事項ではなく、確認して駄目だった事実である。
17. **【解消済み・経緯を残す】** かつて、
    「INSTALL.md が `src/popup.js` の 141 / 355 / 365 / 371-372 行目をアンカー語つきで引用しているため、
    372 行目より前で行数を増やせず、既存3関数への差し込みを『同一行への追記』という
    不自然な書き方にせざるを得ない」という制約があった。
    **2026-09-03 にこの文書の引用を全数付け直し、この制約は解消済みである。**
    引用は行番号だけでなく**アンカー語で内容も照合する**ようになったので、
    実装で行がずれれば `verify.sh` の項目12 が赤くなり、文書側で直せば済む。
    **詰めて書いた3箇所は、普通の書き方に戻してよい。**
18. **★v0.6 の「ホバーで開く／クリックで固定」（§4-11）は実機では全体が未確認。
    導線そのものは 2026-09-03 に自動テストを足したが、YouTube のコントロールバーが
    数秒で自動的に隠れることとの相性は、偽 DOM では原理的に検査できない。**
    バーが消えるときに `mouseleave` が飛ぶかどうかで体感が変わる。
    **この版で最初に判断すべきはここ**（§4-8 項目15 が判定手順）。
19. **★v0.6 の ⚙（パネル内のプリセット編集）と初回ヒントは、content 側とつながった**
    （2026-09-03 実測。§4-12 に根拠を書いた）。bridge に `setSettings` と `markHintShown` が入り、
    mount の options に `firstRunHint` も渡るようになった。
    **前の版のこの項目には逆のことが書いてあった**（出ないほうが正常、という趣旨）。
    いまは**出るのが期待値**で、出なければ不具合である。**読み替えに注意すること。**
    ただし実機で出るところを見た者は居ない（＝未確認であることは他の項目と同じ）。
20. **チャンネル別の音量記憶は廃止した**（「必要ない機能」と判断したため）。§4 の項目12 は
    機能ごと消えている。他文書が項目番号で参照しているため番号は振り直していない。

---

## 10. 記録欄（埋めれば、そのまま issue に貼れます）

```
実施日:
ブラウザとバージョン:
OS / WSL ディストリビューション名:
読み込みパス（UNC / コピー先のどちらを使ったか）:

通った項目番号:
落ちた項目番号:

落ちた項目の詳細（画面に出た文言・コンソールの [YTVP] ログをそのまま）:

verify.sh の結果（pass= fail= と exit=）:
```

**落ちた項目があっても、それは失敗ではなく成果です。**
「どこが壊れているか」が具体的に判明することが、この文書の目的です。
落ちた項目が1つでもあれば、上の記録欄を埋めて issue を立ててください。
