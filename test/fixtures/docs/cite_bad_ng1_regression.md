# NG-1 の再現検体（実際に起きた腐りをそのまま置いてある）

bc0d0da 時点の INSTALL.md にあった記述の写し（表現だけ中立化してある）。引用先は 87-89 行だが、
その3行は jsonClone の実装であって、引かれている一文は 100 行目にある。
**行番号は実在の範囲内なので、範囲チェックだけでは絶対に捕まらない。**
これを捕まえられなくなったら、この検査は NG-1 の再発を止められない。

- **実機で動かした実績はゼロ。あなたが最初の1人です。**
  検証はすべて Node 上の偽 DOM / 偽 chrome で行われた
  （test/helpers/wiring.js:87-89 に「実物の page.js は movie_player と Web Audio に触るので
  Node では動かせない」と明記されている）。
