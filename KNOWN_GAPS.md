# KNOWN_GAPS.md — 既知の穴（正直な在庫）

> この文書は、独立検証（3回）と実装の作業のなかで実測で見つかり、**直さずに残すと決めた**穴の在庫である。
> 「直せるのに直していない」ものと「原理的に直せない」ものを区別して書く。
> 検証を重ねれば穴は必ずまた出る。**全部潰すまで終わらせない設計は、終わらない設計**なので、
> 停止条件（2026-09-02 に宣言）に従いここで線を引いた。
> **この一覧が空であることは、安全の証明にならない。測っていないだけかもしれない。**

最終更新: 2026-09-02（独立検証の最終判定と、その後の修正2件を反映）

## A. 実機・実通信（最大の未確認領域）

1. **ブラウザ実機で確認できているのは一部だけである。**
   自動検査はすべて Node 上の偽 DOM・偽 chrome で、ブラウザのことは何も分からない。
   実機で潰せているのは Chrome の `www.youtube.com` における基本操作
   （パネルの開閉・数値入力・プリセット・プレイヤー内ボタンの表示位置）まで。
   **100 超のブースト（Web Audio）が実際にどう鳴るか・`music.youtube.com`・Firefox は未確認。**
   INSTALL.md §4 のチェックリストが唯一の潰し方で、実施できるのは人間だけ。
   とくに「`tabs` 権限なしで `chrome.tabs.query` が `url` を返す」という SPEC 3-b の前提が
   外れると、設計の存在理由に直撃する。
2. **実際のネットワーク通信をキャプチャした者はいない。** 検査は静的のみ。

## B. 静的検査の原理的な限界（開示済み・設計上残す）

3. **API の文字列再構成は捕まえられない。** `globalThis["fet"+"ch"]`・`window["XML"+"HttpRequest"]`・
   `[93,184,216,34].join(".")` の3種は実測で素通り（README の権限方針3 の開示どおり）。
4. **ベンダリング（他人のコードを src/ に直接貼る）は「依存ゼロ」検査で区別できない。**
   配布 zip がこのソースと同一である保証も無い（README の権限方針4 の開示どおり。再現ビルド・署名照合は未実装）。
5. **ビルド設定の検出はファイル名の列挙。** parcel / swc / turbopack / bun build や、
   CI の YAML 内のワンライナーバンドルは検出できない（README の権限方針4 の開示どおり）。

## C. verify.sh 自身の残存経路（一部は 0fc8d02 で対処済み。残りを列挙）

6. **消毒器そのものを盲にする経路が残る。** `declare` / `unset` を関数で潰されると層1（unset -f）は
   検知も消毒もできない。その場合は層2（署名の空振り検知）だけが残り、実測で9項目が赤くなるが、
   **全項目が赤くなるわけではない**（実測: pass=4 fail=9）。
7. **PATH 乗っ取りは未検査。** PATH の先頭に真面目に偽装した `node` を置かれた場合、
   項目2の陽性対照では捕まらない可能性がある。
8. **署名検知は「検査器が起動して走った」ことしか保証しない。** 検査器の内部の道具（grep 等）を
   殺された場合、署名を出しつつ violations=0 を返す検査器がある（実測: その状況でも別の4項目が赤くなり
   全体は exit=1 だが、個別項目としては緑を返しうる）。この層は項目8（既知の違反検体で赤くなる回帰）が支える。
9. **`--allow-inherited-functions` は逃げ道である。** CI 設定に恒久的に書かれると層1b は無効化される
   （層1の消毒と層2は残る）。この引数を見たら、なぜ要るのかを問うこと。
10. **項目8の pass 下限(100)・項目1の下限(20)は粗い網。** 「検体が丸ごと消えた」を捕まえるためのもので、
    件数の正しさは主張しない。

## D. 文書検査の限界

11. **項目12（引用の腐り検査）は、アンカー語が隣にある引用しか内容一致まで見ない。**
    内訳の実数は `verify.sh` 実行時の `scan_doc_cites:` 行に毎回出る（この文書に数字を書くと腐るので書かない）。
    アンカー語が隣に無い引用の内容腐りは検知できない（scan_doc_cites.sh 冒頭の開示どおり）。
12. **文書中の埋め込みコマンドの正しさを検査する機構は無い。** 最終判定の NG-C
    （文書が読者に打たせるコマンドが1項目取りこぼす）は人手で見つかった。同型は再発しうる。
13. **「内容での引用」（行番号を伴わない実装説明）を検査する機構は無い。** 人手による
    全数照合（26件）が最後の状態で、以後の変更に自動追随しない。

## E. 走査対象の限界

14. **`list_scan_targets.sh` は node が無い環境で、manifest 参照ファイルの収集が警告なく0件になる。**
    コード拡張子の収集だけが残り、`assets/` のような非コード配布物が静かに走査から外れる。
15. **`test/` と `tools/` は走査対象外だが、フォルダ読み込み・ZIP には同梱される。**
    `test/fixtures/` には意図的な違反コードの検体が含まれる（実行はされない。INSTALL.md §7 の開示どおり）。
16. **項目13の列挙のうち4名は未実測**：`npm-shrinkwrap.json` / `bun.lockb` / `makefile`（小文字）/
    `GNUmakefile`。同一ループで処理されるため動く見込みだが、実測はされていない。

## F. 運用

17. **公開・配布を機構で止めるものは無い。** リポジトリを公開する操作もストアへの提出も、
    人が判断して行う。手順としての取り決めであって、機構ではない。
18. **README の過小申告が1件**：`navigator.connection` は検出されるが README の列挙に無い（害は小さい）。
19. **回帰スイートの実行時間は1〜2分**（verify.sh の入れ子起動を含む。環境で動くので幅で書く）。

## G. 絞り込み再判定（2026-09-02）で追加された在庫

20. **`NODE_OPTIONS` 等、node 側の環境フックは消毒対象外。** 消毒しているのは `YTVP_*` とシェル関数のみ。
    実測：壊れる preload（`--require /存在しないパス`）は node 系項目が赤くなり fail-closed。
    **正常に動いて結果を偽る preload は未検査**（検証を行った環境で一時ファイルを作れず測れなかった）。
21. **`BASH_ENV` 経由の関数注入は層1で塞がれている**（実測 exit=1・全項目不採用）。
    塞がっている経路も在庫に書く——次に検証する者が測り直さずに済むように。
22. **PATH 乗っ取り（7番）は2回連続で未実測。** 理由は検証を行った環境で偽バイナリを置けないこと
    （実行環境側のガードによる過検知。別途起票済み）。
    **測るなら回帰スイート側に1ケース作るのが筋。**
23. **verify.sh 項目8 が表示する回帰の pass 数と、回帰を単体実行したときの pass 数は一致しない。**
    verify.sh 経由では `--no-verify-recursion` により verify.sh 自身を入れ子起動するケースが
    SKIP されるため、単体実行のほうが多くなる。差の理由は `run_scanner_regress.sh` 内に明記されている。
    **2つの数字を突き合わせて「減った」と誤読しないこと。**
24. **【2026-09-02 に解消・経緯を残す】検証報告が保存されていなかった。**
    検証3回目まで、報告はどこにも保存されておらず、検証する側は自分の前回報告と
    照合できなかった。同日、本件の全報告21件を受け口に通し
    （受理19・差し戻し2＝受け口側の既知の過検知）、
    **最終確認では保存済みの前回報告を使って照合した。** 以後の報告も同じ経路を通すこと。

## H. v0.2 ネイティブ統合UI（2026-09-02）で増えた在庫

25. **YouTube の実 DOM への依存が1段深くなった。** ネイティブボタンは `#movie_player` 配下の
    `.ytp-right-controls` に差し込む設計だが、**このクラスが現行の YouTube に実在するかは未確認**
    （偽DOMでしか検査していない）。YouTube が構造を変えたら、ボタンは黙って出なくなり
    従来の浮きハンドルに戻る（それが設計。壊れはしないが、気づく機構も無い）。
    探索は content.js の1箇所に集約してある。
26. **overlay が浮きハンドルへフォールバックしたとき、content.js は「ネイティブで挿せた」と
    誤認したままになる**。ユーザーに見える挙動は正しい（ハンドルは出る）ので
    直していない。直すなら mount 戻り値への `native` フラグ追加＝契約変更が要る。
27. **コントロールバーが自動で隠れると、開いたパネルも一緒に消える。** ボタンもパネルも
    コントロール列の中に居るための構造上の帰結。バグではないが、日常使用で許容できるかは
    **実機での使用感の評価待ち**（INSTALL.md §4-8 項目15）。許容できなければ次版で
    パネルだけ外に出す設計変更を検討する。

## I. v0.3 パネル数値入力（2026-09-02）で増えた在庫

28. **パネル内キーの封じ込めはバブリング段でしか効かない。** 実機の YouTube が document への
    **キャプチャ段リスナ**でキーを拾っている場合、stopPropagation では止まらず、入力中に
    数字キーでシークが起きうる。実 DOM でどちらかは**未確認**（INSTALL.md §4-8 項目18 が検証手順）。
29. **IME（日本語入力モード）中の Enter の挙動は未確認。** 変換確定の Enter と適用の Enter の干渉。
30. **オフライン時（`disabled` の入力欄）に Enter が効かないことは未検査**（偽DOMが disabled の
    イベント遮断を模していない）。Esc の取り消し意味論も回帰化されていない。
31. 運用ノート：回帰スイート単体は**4分超**かかる（2分タイムアウトでは死ぬ）。verify.sh 内表示の
    pass は単体実行より**13件少ない**（`--no-verify-recursion` のスキップ分。突き合わせて
    「減った」と誤読しないこと。実例：この数字の食い違いで3回足を止めている）。

## J. 左配置（純正音量の隣）の断念と、その理由（2026-09-03 実機で確定）

32. **YouTube のコントロールバーの中にパネルを置くことはできない。** 実機で確定した事実：
    ボタンを `.ytp-left-controls` に入れると、クリックは効き（`aria-expanded="true"` /
    `ytvp-is-open` を実機で確認）**パネルは開いているのに画面に見えない**。
    コントロールバーが切り落とすか、重なり順で隠れている（原因は未特定）。
33. **純正の音量スライダーはホバーで右へ伸びる**（実測：`span.ytp-volume-area` は畳んだ状態 40x40）。
    その右隣にボタンを置くと、押しに行く途中で押しのけられて**クリックが当たらない**。
    左隣なら干渉しない（これは解決済み。32 が残った）。
34. **偽DOM では 32・33 のどちらも検知できない。** レイアウト・CSS・ホバー・重なりを持たないため。
    **「正しい要素の隣に入った」ことは証明できても、「押せる」「見える」は証明できない。**
    ここが本リポジトリの検査の到達限界であり、実機確認を1つずつ挟む以外に埋める手段が無い。
35. **再挑戦するなら**：パネルをコントロールバーの中に置かず、**プレイヤー直下（`#movie_player`）に
    置いてボタンの座標に合わせて表示する**設計が要る。ボタンだけをバーの中に、パネルは外に出す。
    それは v0.2 の構造（浮きパネル）に近く、**設計を戻すのではなく作り直す作業**になる。

## この在庫の使い方

- ここにある穴を**塞いだら、この文書から消して、回帰に検体を残す**こと。
- 新しい穴を見つけたら、**直すか、ここに書くか**のどちらかにする。黙って知っているのが最悪。
- A-1 だけは他のすべてに優先する。**実機で動かない拡張の検査を磨いても意味がない。**
