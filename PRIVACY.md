# プライバシーポリシー

**YT Volume Precise は、利用者のデータを一切収集しません。**

最終更新: 2026-09-03 / 対象バージョン: 0.2.0

## 収集しないもの

以下を含め、何も集めず、何も送りません。

- 個人を識別できる情報（氏名・住所・メールアドレス・年齢など）
- 健康情報、金融・決済情報、認証情報（パスワード等）
- 位置情報
- 閲覧履歴、開いている URL、視聴した動画
- 操作の記録（クリック、マウス移動、キー入力）
- 端末やブラウザの識別子、利用統計、クラッシュレポート

## 保存するもの（あなたの端末の中だけ）

拡張は次の設定を `chrome.storage.local` に保存します。**あなたのブラウザの中にのみ保存され、
どこにも送信されません。**

- 直近の音量値
- プリセットの数値（既定は 15 / 30 / 50 / 70 / 100）
- ブーストを有効にしたかどうか
- 初回ヒントを表示したかどうか

拡張を削除すれば、これらも一緒に消えます。

## 外部への送信

**ありません。** この拡張のコードは外部通信の手段を1つも持っていません。
`fetch` / `XMLHttpRequest` / `WebSocket` / `sendBeacon` / `RTCPeerConnection` などを含まず、
外部サーバーへの URL も持ちません。これは文章上の約束ではなく、リポジトリの検査
（`verify.sh` の通信スキャン）が機械的に検査しており、破ると検査が失敗します。

ただし正直に書くと、この検査は**既知の API 名の字面を探す仕掛け線**であって、
数学的な証明ではありません。文字列を組み立てて API を再構成するコードは原理的に捕まりません。
その限界はリポジトリの README と KNOWN_GAPS.md に開示しています。

## 要求する権限と、その理由

| 権限 | 理由 |
|---|---|
| `storage` | 上記の設定を端末内に保存するため |
| `*://www.youtube.com/*` `*://music.youtube.com/*` | YouTube のプレイヤーの音量を読み書きし、プレイヤー内に操作パネルを表示するため |

**`<all_urls>` も `tabs` も要求しません。** YouTube 以外のページでは、この拡張は一切動きません。

## 第三者への提供・販売

収集していないため、提供も販売もしません。

## ソースコード

すべて公開されています。上記の主張は、コードを読んで確かめられます。

https://github.com/erenoa-6621/yt-volume-precise

## 連絡先

不具合・質問・このポリシーへの指摘は、GitHub の Issues でお願いします。

https://github.com/erenoa-6621/yt-volume-precise/issues
